services:
  - type: web
    name: tricks-generator
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host=0.0.0.0 --port=$PORT"
    envVars:
      - key: TRANSFORMERS_CACHE
        value: /tmp
